//---------------------------------------------------------------------------

#ifndef Unit5H
#define Unit5H
//---------------------------------------------------------------------------
#endif

class list
{
  public:
	int field;
	list *ptr;
	list* addelem(list *lst, int number);
	list* init(int a);
};