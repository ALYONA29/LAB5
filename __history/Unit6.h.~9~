//---------------------------------------------------------------------------

#ifndef Unit6H
#define Unit6H
//---------------------------------------------------------------------------
#endif

#include "Unit5.h"

class queue : public list
{
  public:
	list *frnt, *rear;
	void initiz(queue *q);
	int isempty(queue *q);
    void insert(queue *q, int x)
};